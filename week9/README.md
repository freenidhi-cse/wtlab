#### 9a) Using cookies to store usernames and passwords, followed by authentication ( not exactly the right approach )
#### 9b) Storing user details in web.xml, using getInitParameters() and performing authentication
